/// <reference types="node" />
import { ChunkFileModel } from '@sidetree/common';
import CreateOperation from '../CreateOperation';
import RecoverOperation from '../RecoverOperation';
import UpdateOperation from '../UpdateOperation';
/**
 * Defines schema of a Chunk File and its related operations.
 * NOTE: Must NOT add properties not defined by Sidetree protocol.
 */
export default class ChunkFile {
    /**
     * Parses and validates the given chunk file buffer and all the operations within it.
     * @throws SidetreeError if failed parsing or validation.
     */
    static parse(chunkFileBuffer: Buffer): Promise<ChunkFileModel>;
    private static validateDeltasProperty;
    /**
     * Creates chunk file buffer.
     */
    static createBuffer(createOperations: CreateOperation[], recoverOperations: RecoverOperation[], updateOperations: UpdateOperation[]): Promise<Buffer>;
}
