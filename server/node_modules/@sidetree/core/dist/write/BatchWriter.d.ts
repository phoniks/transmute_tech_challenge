import { IBatchWriter, IBlockchain, ICas, IOperationQueue, IVersionMetadataFetcher } from '@sidetree/common';
/**
 * Implementation of the `IBatchWriter`.
 */
export default class BatchWriter implements IBatchWriter {
    private operationQueue;
    private blockchain;
    private cas;
    private versionMetadataFetcher;
    constructor(operationQueue: IOperationQueue, blockchain: IBlockchain, cas: ICas, versionMetadataFetcher: IVersionMetadataFetcher);
    write(): Promise<void>;
    private getNumberOfOperationsAllowed;
}
