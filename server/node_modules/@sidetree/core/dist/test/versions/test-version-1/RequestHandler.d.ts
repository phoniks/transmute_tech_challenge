/// <reference types="node" />
import { IOperationQueue, IRequestHandler, ResponseModel } from '@sidetree/common';
import Resolver from '../../../Resolver';
/**
 * Request handler.
 */
export default class RequestHandler implements IRequestHandler {
    private resolver;
    private operationQueue;
    private didMethodName;
    private supportedAlgorithms;
    constructor(resolver: Resolver, operationQueue: IOperationQueue, didMethodName: string, supportedAlgorithms: number[]);
    handleOperationRequest(request: Buffer): Promise<ResponseModel>;
    handleResolveRequest(didOrDidDocument: string): Promise<ResponseModel>;
}
