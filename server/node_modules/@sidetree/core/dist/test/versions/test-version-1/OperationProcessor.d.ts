/// <reference types="node" />
import { AnchoredOperationModel, DidState, IOperationProcessor } from '@sidetree/common';
/**
 * Operation processor.
 */
export default class OperationProcessor implements IOperationProcessor {
    apply(operation: AnchoredOperationModel, didState: DidState | undefined): Promise<DidState | undefined>;
    getRevealValue(anchoredOperationModel: AnchoredOperationModel): Promise<Buffer>;
}
