export default class MapFile {
    _id?: string;
    chunks: {
        chunk_file_uri: string;
    }[];
    operations?: {
        update: {
            did_suffix: string;
            signed_data: string;
        }[];
    };
    constructor(chunks: {
        chunk_file_uri: string;
    }[], operations?: {
        update: {
            did_suffix: string;
            signed_data: string;
        }[];
    });
}
